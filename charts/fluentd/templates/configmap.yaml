apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fluentd.fullname" . }}
  labels:
    {{ - include "fluentd.labels" . | nindent 4 }}
data:
  fluent.conf: |
    <label @FLUENT_LOG>
      <match fluent.*>
        @type null
      </match>
    </label>
    {{- with .Values.extraConfig }}
      {{- . | nindent 4 }}
    {{- end }}

